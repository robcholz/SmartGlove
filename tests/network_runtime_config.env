# This file is embedded into `src/bin/provision_network.rs` at compile time.
# The E2E test rewrites these values to point at the local Python test server before building.
DEVICE_INFO_URL=http://127.0.0.1:8787/v1/device-info
STATUS_WS_URL=ws://127.0.0.1:8787/v1/ws
EVENT_NAME=network.ready
EVENT_PAYLOAD_JSON={"phase":"provisioned"}
